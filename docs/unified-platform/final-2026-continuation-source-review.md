# Final 2026 continuation source review

Review date:2026-09-19. This report contains policy and implementation evidence;
it contains no member identities, credentials held by individuals, raw workbook
rows or private relationship facts. It is not a release or Real-activation
certificate.

## Fresh source verification

All four final inputs were freshly hashed and match
[the final source manifest](final-2026-source-manifest.json). The nine-page final
PDF and relevant calculation and MASTER cells were re-extracted read-only.
The fresh228-row MASTER extraction exactly matches the prior verified extract.
The parent separately confirmed the captured production export had not changed.

Main AirTech requires FF/DE, valid Scott SCBA, Cylinder Hazmat/FSO and DE.
Backup requires FF/DE and DE; Scott and Cylinder are preferred. PDF p4
Procedure7(a)'s final minimum-qualified reverse-seniority sentence governs this
specific fallback, consistent with the user continuation. `Points!CY5`'s OR and
`Rules & Points!B80`'s mandatory-carseat placement are intentional oracle
divergences. Carseat's one-point preference is supplied by `A88:C88`.

Events Captain's four PDF p1 Procedure3(b) preferences are cumulative binary
criteria, with no unequal weights or priority inferred from bullet order.
Prevention Lieutenant's RN8977 I AND II is one combined criterion; the workbook
omits it. Backup Scott/Cylinder preferences are also binary cumulative criteria.
The new reusable scoring representation records a source reference and counts
each satisfied criterion once. Existing explicit numeric groups remain unchanged.

Investigator IAAI-CFI is a separate ordered qualification tier, before cumulative
credits. PDF p2 Procedure3(e)(i) limits the three secondary-course preferences
to non-IAAI candidates. An explicit held-qualification exclusion prevents those
courses from deciding an IAAI-versus-IAAI tie. No large numeric tier multiplier
is invented. Canonical comparator and interruption guards have focused synthetic verification;
production acceptance remains separate.

The DE A-Day scope is resolved: eight source-designated positions per shift,
suffixes103,104,202,303,304,402,707,708;24 total. This includes Combat Float DEs
and excludes AirTech203 and Marine Operator602/Engineer603 solely holding DE.
Maximum2 per group is explicit in PDF p7 Procedure12.

The user also resolves the workbook ordinal mapping: Rank Seniority is the
source-certified time-in-grade bid ordinal; Straight Seniority is the
source-certified Department-service bid ordinal. See
[the ordering decision record](ordinary-bid-ordering-decision-record.md).
No hire/promotion date or personnel lifecycle field is inferred.

## Semantic topology crosswalk

| Final228 classification | Count |
| --- | ---: |
| Exact continuation |133|
| Material role/scope change |47|
| New position |34|
| Ambiguous old-identity or source mapping |14|

Thirty-two exact continuations change source numbers. Captured old242 identities
classify as133 continued,96 retired topology identities and13 candidates held
for ambiguous matching. Retirement here does not modify assignments or people.

The14 final ambiguities comprise six Marine float seats and six Rescue float
Lieutenant seats without authoritative individual ordinal pairing, plus B214
and C214 where Division Rescue conflicts with the Combat role suffix. These
narrow mappings require review only if carrying old identities/bindings; distinct
new identities do not automatically inherit staffing. The division conflict
still requires a source decision. There is no blanket request to review all228.

Investigator final305 uniquely continues old303's semantic role. The final
MASTER source unit Combat3 remains documented while the effective assignment is
explicitly Ladder3 by PDF pp2/7. Final305 cannot inherit old305's Lieutenant
binding. Final211 in all shifts is DivisionChief/unit300; captured inherited
staffing links do not themselves establish approved applicable-rank bindings.
No approved usable staffing bindings were established for this draft.

## Generated canonical draft and evidence

The ignored deterministic builder produces a schema-valid canonical definition
with228 positions,228 source profiles and223 compiled/decoded biddable rules,
24 homogeneous pools,10 qualified fallback policies,2 term groups,5 pending stages and
3 A-Day scopes. It also includes an interrupting Investigator specialty with
ordered qualification metadata. All source concepts are preserved in pending
policy/source decisions even when execution facts are absent.

`settings` and strict execution `policy` remain null. `pendingPolicy` contains
empty actor/member lists and null source-unknown numeric limits. No actual
operator grant, member employment state, service interval, ordinal reconciliation,
qualification validity or staffing binding is fabricated.

Bounded local evidence collected during this continuation:

- Shared source-aware preference schema:12 tests passed.
- Eligibility cumulative preferences, exclusions and existing scoring/completion
  regressions:17 tests passed.
- Preference editor authoring/preservation:3 tests passed.
- Compiled final-policy goldens and scoped A-Day tests:109 tests passed.
- Generated canonical artifact:223 biddable rules compile and decode while228 source
  profiles remain preserved;86 synthetic source
  assertions pass against its actual rule/scoping material.

These counts describe distinct bounded checks, not a claim of full production
coverage. Ordered comparison/canonical interruption, full source-derived Mock,
release CI and authenticated runtime acceptance have separate owners/evidence.

## Remaining scoped decisions

SWAT's six selected medics, two per shift, separate A-Days, rank/unit independence
and training/deployability requirements are clear. Current-six-only versus a
wider qualified pool remains a typed unresolved population decision. MASTER
also does not separately identify a Backup AirTech seat or membership.

The referenced Timeline, current Chief supplemental requirements and Article5
remain absent in the previously documented search scope. SOG200.17 was found;
private relationship facts remain separate. Approved bid dates, operational
settings, contact-count decisions, general A-Day limits, actor authority,
identity-safe ordinal evidence, qualified credential facts, term/service facts
and staffing bindings remain activation evidence. Marine's three-month DRI
obligation needs the approved bid start date, not a substituted award date.

The draft records these as `BLOCKS_REAL_BID_ACTIVATION` with affected scopes.
They are not automatically `BLOCKS_APPLICATION_RELEASE`. Saving reviewable
pending material, passing synthetic tests or producing a crosswalk does not make
the final2026 configuration complete or authorize a Real Bid.

The exact generated draft subsequently passed the real canonical Save path in an
in-memory copy of the fresh captured production database: Save, exact retry and
version load preserved pending policy, produced228 positions and223 active rules,
created zero Real sessions, and passed foreign-key and SQLite integrity checks.
This is a local rehearsal only. The initial all228-rule schema-only proposal was
corrected to retain closed source profiles without active rules. Mixed DE/FF and
LT/FF pools were split into24 homogeneous capacity buckets; validation was not
weakened. Six focused profile-materialization tests pass, including closed-profile
preservation and rejection of targets outside the actual definition.
