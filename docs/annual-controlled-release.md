# Controlled annual-platform release — 2026-09-07

This procedure follows the existing separate Release Captain migration path: ordinary staging and production deployment remain read-only against D1. The user explicitly requested completion and live production deployment on September 7. It does not authorize personnel reconciliation or a Real Bid acceptance test.

1. Pin the reviewed source SHA/tree, complete repository and browser gates, and check no competing release or active Real Bid.
2. Capture existing Worker/Web deployment identities, the exact migration ledger, a fresh private Time Travel bookmark and the existing R2 backup workflow result. Download the backup, compare its hash to an independent export, and rehearse import plus the migration chain against the private production copy. Keep raw data, credentials and bookmark values outside Git.
3. Use the canonical managed Wrangler migration mechanism with a private, hash-verified migration directory exposing only the already applied prefix plus the next reviewed migration. Do not edit canonical SQL or insert ledger rows manually. Apply 0045–0055 in order, one migration per managed invocation. After each invocation, require the exact expected ledger, a clean foreign-key check, preserved protected table counts and no active Real Bid. Stop on a mismatch; never continue blindly or reseed.
4. Coordinate this short schema cutover with the validated application's Worker deployment. The old credential rename writer is incompatible with the new immutability trigger; its failed transaction stays atomic. Do not use the old Worker as a recovery target against candidate-written data. Preserve the compatible candidate source/artifact and use a tested forward fix. Database restoration is an incident action requiring an accounted-for write window, not an automatic fallback that discards administrative writes.
5. Run the existing guarded production deployment workflow for the immutable candidate. Its own ledger guard must pass without a D1 mutation. Record Worker/Web versions and hosted build evidence; verify production health, disabled writeback and normal Hub SSO.
6. Exercise production preparation, navigation, all three board views, roster/export and isolated Mock/read-only live surfaces. Use local or staging fixtures for changes to personnel, seats or policy; no Real Bid operations. Reconcile production source with main and retain a separate acceptance record.

The September 7 pre-release rehearsal imported the actual production export, applied eleven migrations and preserved all 8,934 existing rows across 49 tables, including original column values, with clean foreign keys after each step. Backup workflow 34082352610 succeeded. Its retrieved R2 SQL and the independent export share SHA-256 `950a0ee7c86b89fdff65d3b67d69b48dd7dd89a6aba0e44f16bcc73a7e67093b`. These are completed preparation checks, not a claim that the remote migrations or release have already run.
