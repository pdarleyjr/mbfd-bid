# MBFD Bid v2 implementation log

## 2026-08-27 — final staging auth hardening checkpoint

- The exact Web source `5ead0c7df4bd1e3b69b19ea17ae0ff7c648fdd15` passed focused auth/PIN tests, frozen local validation, `pnpm lint`, typecheck, build, D1 backup preflight, and the full suite (1,102 passed; 4 intentional opt-in skips). The clean Linux Node 22.22.1 / pnpm 9.12.0 gate also passed frozen install, lint, package builds, typecheck, OpenNext staging build, and local preview smoke before the exact Web artifact was deployed as staging version `c1d49ee3-a9ab-4531-b65b-5275617345cc`.
- The staging API and OpenNext Web Workers received one newly generated shared JWT signing key without retrieving or recording any existing value. Name-only verification was followed by fresh API login, API JWT verification, same-origin Web session finalization, secure cookie installation, and authenticated `/admin` access. The current API secret-change version is `aadd7371-417c-4a4c-9a01-d0a5dcccd4cf`; no D1 migration was applied for the auth work.
- The staging-only local `admin` password was reset as a bcrypt digest on the API Worker. Its plaintext is available only through a user-context DPAPI-protected local handoff, not source, documentation, logs, or the PR.
- `settings:member_bid_pin` is now explicitly configured as the canonical staging PIN **2300**. It is not a fallback: missing, malformed, or unavailable KV fails closed. Browser acceptance changed the PIN to a random temporary value through **Admin → Settings → Bid Access PIN**, proved old/new behavior, then restored 2300 through the same UI. The obsolete staging `PIN_HASH` secret and obsolete temporary PIN handoff were removed after the final proof.
- Web `POST /api/auth/session-finalize` and `POST /api/pin` now use the same exact-origin / Fetch Metadata guard pattern as logout and bootstrap. Regression tests cover foreign/cross-site rejection without cookie mutation.
- Authenticated browser acceptance passed with zero console errors and zero observed 5xx responses: secure cookies survived reload; all 13 implemented admin destinations rendered; the existing mock board opened an authenticated WebSocket and received a state snapshot; audit-chain verification passed; an existing staging audit export retrieved successfully; and a portal retry reached the expected disabled `409` before any outbound publication.
- A new mock session was created as mock-only and an authorized auto-bid attempt correctly stopped with `active_rule_book_invalid` before any pick. The active `2026.1` rule book has 232 source rows but only 229 semantically valid decoded rules. No policy/rule data was modified; successful mock-pick acceptance remains blocked pending accountable rule-data correction. Member acceptance remains pending because no authorized member identity was supplied.
- GitHub Actions were not invoked, dispatched, rerun, waited on, or used. They were unavailable for this checkpoint; all claims above are local, direct staging, or browser evidence. No production, GMKtec, Docker, Media Control, DNS, tunnel, or shared-host change was made.

## 2026-08-27 — staging release checkpoint

- Deployed the reviewed feature source `22759202dce37b3073b06164c46c22e424731691` to staging only. Exact local gates passed: frozen install, lint, package builds, typecheck, Next build, D1 preflight, `pnpm test` (175 files, 1,058 passed, 4 opt-in skips), and `git diff --check`.
- Independent clean Linux proof passed with Node 22.22.1, Next 15.5.24, and OpenNext 1.20.4. The emitted OpenNext artifact had 1,514 files, no native `.node` files, and no textual `extract-zip` or `@puppeteer/browsers` reference. A disposable Workers canary passed root, static-asset, and auth-boundary checks before deletion; the same artifact now serves the staging custom domain as Worker version `52741a16-8c57-47d0-ad67-fade8801c270`.
- Deployed API Worker version `19f9d4b1-74a7-46e9-9657-f973853a0379`. It binds `R2_AUDIT` to `mbfd-bid-audit-staging-v2` and `R2_EXPORTS` to `mbfd-bid-exports-staging-v2`, sets `R2_EXPORTS_BUCKET_NAME` accordingly, keeps portal writeback disabled, and has no `PORTAL_BID_WRITER` secret. The portal queue remains paused with one producer and zero consumers; no portal write was attempted.
- Captured a fresh private staging D1 Time Travel bookmark and export immediately before API deployment. The remote ledger already contained migrations 0001–0022, managed apply reported no pending migration, and `foreign_key_check` was empty. A disposable Bid-only database completed managed migration, import/count/FK comparison, and proof-only Time Travel restoration; the disposable databases and the 16 named local proof files were deleted afterward. This is recovery evidence, not a claim of physical-bit erasure.
- The existing `mbfd-bid-web-staging` Pages project was retained as rollback material. Its domain/DNS attachment was not detached or recreated; the active staging custom domain now targets the OpenNext Worker.
- Anonymous Playwright acceptance passed for the staging banner, PIN gate, anonymous admin redirect, static asset, live logout endpoint, and a zero-error console. The logout endpoint returns `204` and clears both `mbfd_bid_jwt` and `mbfd_pin`. No dedicated staging Bid PIN or safe admin/member test credentials were available, so authenticated admin/member, WebSocket, mock rehearsal, export, and portal-suppression acceptance remain explicitly unobserved.
- Exact-head `pnpm audit --prod --audit-level=high` has one unresolved high advisory: transitive `extract-zip@2.0.1` through `@cloudflare/puppeteer@1.1.0`. No patched release is published. This was not suppressed; emitted-artifact evidence is staging reachability evidence only, and production promotion remains blocked on the unresolved security/policy/operational gates.
- No production, GMKtec, Docker, Media Control, tunnel, or shared-host change was made.

> Earlier dated entries below are retained as point-in-time history. Their prior
> statements about an unmerged OpenNext spike, Next 15.5.21, two advisories,
> local-only recovery proof, or undeployed staging migrations are superseded by
> this checkpoint for current staging status.

## 2026-08-26 — Phase 0 baseline

- Captured local repository, GitHub, Cloudflare, GMKtec, and Media Control read-only baseline.
- Created local branch `feat/mbfd-bid-v2` from `beed7412f608306ff411b7a9a603bc3b8d75cdb3`.
- Recorded a clean locked install, lint, typecheck, build, and root test pass. The first Worker test run timed out in five launcher tests; the five launcher tests now run serially in a dedicated configuration while interactive watch retains coverage, and the current root test run passed 152 files, 878 tests, with 4 intentional opt-in skips. CI evidence remains unobserved.
- Changed the GitHub repository from public to private and verified the result.
- Created source, policy conflict, security, rollout, Cloudflare, test, and Media Control protection contracts.
- Identified the D1-backup CI root cause as an unset Ubuntu `TEMP` environment variable before any Cloudflare call; implemented a local-only cleanup-safe preflight and no-network regression test covering both successful and failed cleanup, and added that test to CI. A real staging backup and disposable restore remain unproven.
- Applied supported dependency updates on the isolated branch, reducing the current production audit from 31 advisories to 5. The remaining transitive advisories are explicitly retained as blockers.
- Fixed the bid reconnect path so a disconnected socket cannot create a false pending pick; added focused unit coverage for disconnected, rejected, and accepted send outcomes.
- Made no server, Docker, Media Control, Cloudflare, DNS, tunnel, D1, KV, R2, Queue, Worker, Pages, or deployment change.

## 2026-08-26 — readiness and dependency follow-up

- Added a pure shared readiness contract for the directive's 16 named checks. Missing, duplicated, and unknown runtime status facts fail closed; warnings remain visible without becoming an implicit block.
- Added a read-only per-session readiness report and changed the non-mock start route to return `409 readiness_blocked` before any D1 transition or audit write while the fact providers remain unconfigured. Mock rehearsals remain available only after a fresh-step-up, pre-start, no-picks designation; the route attempts a separate audit write afterward, so transactional audit coupling remains unproven. This is an interim safety control, not a complete canonical live-command path.
- Removed stale active-web references that advertised a retired AI advisory and stopped invalidating the retired query key on bid events. No AI service or infrastructure was introduced.
- Applied a locked `ip-address` override and verified a frozen install. The production audit now reports two high-severity transitive findings (`sharp` and `extract-zip`); a Next-only update is blocked by the deprecated Pages adapter's unsupported peer range.
- Added a typed persisted-rule decoder at Worker HTTP evaluation/mutation and rule-publication boundaries. It maps the authoritative `ops_all_6` serialization to an explicit all-operations Technician gate, normalizes outer whitespace only, and rejects malformed, conflicting, unmodeled, empty, or duplicate rule books. Draft mutations advance a revision precondition, so publication fails closed if a rule changes after full-book validation; the status swap avoids dependence on SQLite partial-index update order. Rule-book creation plus cloning now shares one D1 batch, so a predicted draft version cannot be published from a partial clone. Member eligibility binds to the active policy for its session year; an admin preview with no version now fails closed if multiple active annual books exist. It intentionally blocks the current `pre_bid_pool` source rows from publication until their semantics are authorized. The current Durable Object pick handler remains outside this guard and is explicitly blocked from live enablement.
- Worker verification passed locally: 100 deterministic files / 559 passed / 1 opt-in skip, plus 5 serial launcher files / 24 passed. Shared readiness tests passed 16 files / 129 tests; web unit tests passed 15 files / 64 tests; eligibility tests passed 84 with 3 intentional opt-in skips. The current root suite passed 156 files / 922 tests / 4 opt-in skips. Browser, CI, D1 backup/restore, Cloudflare, and operational acceptance remain unobserved.

## 2026-08-26 — remote checkpoint and recoverability evidence

- Pushed only `feat/mbfd-bid-v2` and opened draft PR [#96](https://github.com/pdarleyjr/mbfd-bid/pull/96) to `main`; no merge was performed. Its regular pull-request CI completed successfully. CodeQL completed analysis but failed while uploading because GitHub code scanning is disabled for this private repository. That is a repository-settings gate, not a successful CodeQL result.
- A full-history gitleaks scan found seven generic-pattern matches. All were reviewed as test fixtures or test-only idempotency values and classified `FALSE_POSITIVE`; no raw value is recorded. The branch diff added no email, SSN, telephone, or date-of-birth-like material.
- Captured a current staging D1 Time Travel bookmark without recording its raw value. `d1 info` did not expose a backend version, so that command alone does not prove production-backend status. A remote staging export was imported into a disposable Bid-only D1. Schema objects, migration ledger, all queryable application-table counts, and foreign-key checks matched before migration. Migration 0020 was then applied only to the disposable database and its Time Travel restore returned it to its pre-migration shape. The disposable database was deleted. Staging was not restored, migrated, seeded, or otherwise mutated.
- The direct disposable execution of migration 0020 did not update the Wrangler migration ledger. Managed-migration tooling remains a staging-deployment blocker. The sensitive temporary SQL export remains pending secure manual deletion after this environment refused an otherwise scoped removal; no R2 backup was created because Bid-only R2 ownership was not established.
- Added the un-deployed, additive 0021 staffing/import foundation: separate authorized staffing positions, observed member assignments, staged imports and rows, aliases, and service history. The source A/R-day field is explicitly distinct from legacy Bid A-Day semantics; identity matching reserves a keyed opaque HMAC reference. The foundation does not parse TeleStaff data, create current assignments, infer capacity, or create bid opportunities.
- Added a rehearsal-only `mock.freeze` command-boundary proof. Its authenticated adapter checks the existing mock designation before contacting the named Durable Object; the DO then commits state and a private command receipt in one local transaction, replays exact duplicates, rejects reused command IDs and stale sequences, and never writes a D1 freeze projection. It leaves the live freeze route and direct-D1 routes unchanged. D1/R2 audit work remains best-effort after DO acceptance and cannot be represented as audit atomicity.
- The isolated OpenNext compatibility spike passed a Linux/WSL build with Next 15.5.24 and `@opennextjs/cloudflare` 1.20.3, producing a Worker artifact. Its Windows-native OpenNext bundle failed at the `sharp` binary boundary, so Linux CI/WSL is the required build gate. Vinext's official non-mutating check reported 94% source compatibility, but Vinext targets a Next 16 migration and is not the safe current path for this Next 15 application. The spike is not merged, deployed, or runtime-verified.
- Made no Media Control, server, Docker, Cloudflare route/DNS/tunnel, Worker, Pages, staging deployment, or production change. The only cloud resource lifecycle change was creation and deletion of the disposable Bid-only D1 used for recovery validation.

## 2026-08-26 — migration 0021 canonical staffing correction

- Re-verified the clean `feat/mbfd-bid-v2` handoff at `280e8ab4d8e6a008344b77cd34eff6f07a9f147b`: the local tracking branch and draft PR #96 both matched, with `main` as the base. A frozen install, lint, typecheck, build, and root test pass reproduced before this correction. The build correctly exposed that this branch still used unpatched Next 15.5.21.
- Replaced the un-deployed migration 0021 design in place. `staffing_positions` now has an MBFD-owned `stable_slot_key` and no source-system identity/version/hash or source A/R Day. The source representation moved to effective-dated complete locator/signature mappings, preventing a repeated short source label from becoming a global identity.
- Added staged resolved member/mapping references, source A/R Day, normalized source topology, review status/reason evidence, and source-backed immutable `assignment_observations`. Recast `member_assignments` as effective-dated authoritative records with generic provenance (`TELESTAFF_IMPORT`, `BID_AWARD`, `MID_CYCLE_VACANCY`, `ADMIN_TRANSFER`, `PROMOTION`, or `CORRECTION`), so a future award no longer requires a TeleStaff import.
- Corrected the legacy audit-log rebuild to use D1-compatible `defer_foreign_keys` rather than leave a raw SQLite connection with foreign-key enforcement disabled. A pristine full-migration test now asserts that enforcement remains enabled. Source versions reject common surrounding whitespace, non-null signatures/hashes/HMACs/fingerprints are text-only lowercase hexadecimal values, and all operational effective dates are valid ISO calendar dates. Added regression coverage for the revised pristine schema, no-global-alias design, non-overlapping source-mapping ranges, committed-row-count integrity, persistence-level fail-closed review gates, immutable chain-of-custody observations, append-only TeleStaff materializations, canonical-slot authorization, input-format rejection, and pre-TeleStaff bid awards. The shared reconciliation contract now fails closed for blocking rows and review-required rows without an explicit human disposition. Focused Worker (25) tests and Worker TypeScript checks passed locally.
- Ran `wrangler d1 migrations list/apply` with `--local` and a fresh isolated Worker persistence directory. Wrangler 4.93.0 applied all 21 migrations, recorded both `0020_rule_book_revision.sql` and corrected `0021_staffing_assignment_drafts.sql` in `d1_migrations`, then reported no pending migrations, `foreign_keys = 1`, `defer_foreign_keys = 0`, and an empty foreign-key check. This is managed local migration evidence only; it does not prove a staging-derived remote restore, remote disposable D1, Time Travel behavior, or staging safety.
- Final local verification passed `pnpm lint`, `pnpm typecheck`, `pnpm test` (161 files, 965 passed, 4 opt-in skips), and `pnpm build`. The current branch build is still Next 15.5.21. The updated source has not been committed, pushed, or sent through hosted CI; the prior PR run remains evidence only for its earlier remote SHA.
- A bounded Windows-only no-route OpenNext attempt kept the isolated spike clean, credential-free, and non-routable. It compiled/type-checked and generated 20 pages under Next 15.5.24, then failed bundling `sharp` native modules before preview or browser startup. No Worker, Pages, Cloudflare resource, route, or browser runtime was started or contacted.
- No staging migration, D1/KV/R2/Queue mutation, Worker/Pages deployment, Cloudflare route/DNS/tunnel change, server/container change, or Media Control action occurred in this correction.

## Next checkpoint

Resolve the CodeQL repository setting, complete the isolated adapter spike on a supported Linux-compatible path, and keep staging migrations blocked until managed migration metadata and deployment ownership are proven. Obtain the approved sanitized TeleStaff baseline before implementing a parser or commit workflow.
