# Annual data readiness and final staging rehearsal

## Objective

Establish a real 2026 authoritative staffing source, validate and reconcile it through the audited TeleStaff workflow, designate and freeze a separately identifiable staging configuration, then run the final mock rehearsal without direct D1 seeding or production interaction.

## 2026-08-31 update

The controlled staging sequence completed mock disposition, backup retrieval/hash verification, migrations through `0037`, exact-candidate Linux/OpenNext deployment, and fresh PIN/API/browser authentication. Production remains explicitly out of scope. The remaining staging acceptance gate is a real annual-data dependency: no designated annual configuration or accepted authoritative staffing baseline exists, so a fresh mock must not be created by direct D1 mutation.

## Boundaries

Do not alter migration SQL or the D1 ledger, deploy the reviewed application candidate, touch production, enable portal writeback, create `PORTAL_BID_WRITER`, or change MBFD Hub, Media Control, GMKtec, Docker, tunnel, DNS, or shared infrastructure. Do not convert the available Excel XML export, import synthetic/2025 data, or seed any baseline/configuration/session directly in D1.

## Current progress

The ordinary staging deployment workflow has a read-only D1-ledger guard and no migration-apply command. Annual readiness stopped without mutation: the authorized source search found only a May Excel XML export and a 2026 vacation XML, whereas the operator workflow requires the original current TeleStaff Assignments HTML report. The 2026 rule documents further require an operator decision on Fire Investigator inclusion and Station 6 dashboard classification before a staging-only configuration can be designated. Existing stale mocks remain closed.

## Required validation

After the operator inputs arrive: preview the raw official HTML with an explicit snapshot date; reconcile every row; apply and accept the authoritative baseline; record the two policy decisions; create a clearly labeled staging-only rule book/configuration; freeze the mock snapshot; exercise start, pause/resume, pick/decline/skip, export/print, socket receipt/reconnect, close-mock, and post-close assertions; then verify portal writeback remains disabled. GitHub-hosted Actions remain an unavailable evidence gate until account minutes recover.

## Preserved reliability checkpoint

The prior checkpoint covered the Cloudflare Vitest / Durable Object reliability candidate: Workers Vitest v1 migration, genuine Durable Object eviction, and standard-WebSocket recovery coverage. Its non-goals were all staging, production, portal-publication, GitHub Actions, remote D1, deployment, MBFD Hub, Media Control, GMKtec, Docker, tunnel, and DNS changes. It required local deterministic gates, managed local D1 proofs, native Linux/OpenNext root-200 proof, and a P0/P1-free read-only review before its separate staging D1 migration-risk architecture review.
